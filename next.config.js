/** @type {import('next').NextConfig} */
const nextConfig = {
reactStrictMode: true,
images: {
domains: [], // Add external image domains here if needed
},
// Experimental features, rewrites, redirects can go here
};

module.exports = nextConfig;
